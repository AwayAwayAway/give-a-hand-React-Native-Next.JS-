import React from "react";
import {StyleSheet, Text} from "react-native";

const DOTopic: React.FC<any> = (props) => {
  return <Text>Dev-ops Topics</Text>
};

const styles = StyleSheet.create({});

export default DOTopic;