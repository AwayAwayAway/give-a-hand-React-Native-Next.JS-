import React from "react";
import {StyleSheet, Text} from "react-native";

const FETopic: React.FC<any> = (props) => {
  return <Text>Front-end Topics</Text>
};

const styles = StyleSheet.create({});

export default FETopic;