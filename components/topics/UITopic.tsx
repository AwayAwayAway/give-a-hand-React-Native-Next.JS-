import React from "react";
import {StyleSheet, Text} from "react-native";

const UITopic: React.FC<any> = (props) => {
  return <Text>UI/UX Topics</Text>
};

const styles = StyleSheet.create({});

export default UITopic;